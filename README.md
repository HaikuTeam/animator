# sdk-creator

SDK into Haiku.

## Setup

After cloning the repo:

```
$ yarn install
```

Then develop with:

```
$ yarn run develop
```

## Tests

Run the tests with:

```
$ yarn run lint && yarn test
```
