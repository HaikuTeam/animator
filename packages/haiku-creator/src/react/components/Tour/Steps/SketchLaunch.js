import React from 'react'

export default function ({ styles }) {
  return (
    <div>
      <h2 style={styles.heading}>Working with Sketch</h2>
      <div style={styles.text}>
        <p>Let’s check out the always-connected workflow from Sketch to Haiku.</p>

        <p>Double click the Sketch file to launch Sketch, then change the color of Percy’s nose and choose File —> Save in Sketch.</p>
      </div>
    </div>
  )
}
