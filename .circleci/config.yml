version: 2
jobs:
  build:
    branches:
      only:
        - staging
        - production
    machine:
      node:
        version: 8.4.0
      xcode:
        version: 8.2
    dependencies:
      override:
        - yarn
      cache_directories:
        - ~/.cache/yarn
    steps:
      - checkout
      - run: brew install yarn
